# Allow external connections
listener 1883
protocol mqtt

# WebSocket listener on port 9001
listener 9001
protocol websockets

# Allow anonymous connections (for local testing only)
allow_anonymous true

# Logging (for debugging purposes)
log_dest stdout
log_type all